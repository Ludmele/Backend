//While --> usado quando é conhecido
///o teste de condição ocorre antes que o laço seja executado
////só para se for false

    // do {
    //declaração} while(condition);

//For --> sabe  o número de iterações
    /// for(let index = 0; index < array.leght; index++) {
    // const elemet = array[index]}

    ///(inicialização, avalia, incremento ou decremento) {aramzenamento}


///break interromper o laço de repetição -> encontre uma única opção
///continue ---> reinicia uma instrução while, do-while ou for ----> várias opções


///for...in ---> para objetos, percorrendo todas as suas propriedades
///for...of ----> não precisa de um contador para inicializar, ela é usada para iterar coleções, exemplo, coleção de objetos.